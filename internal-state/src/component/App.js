import React from 'react';
import Form from './Form.js';

export default class App extends React.Component {
  render() {
    return (
      <React.Fragment>
        <Form />
      </React.Fragment>
    );
  }
}